// function animateOnScroll(elements, animationClass) {
//   elements.forEach(element => {
//       const elementTop = element.getBoundingClientRect().top;
//       const windowHeight = window.innerHeight;
//       const triggerOffset = windowHeight / 2;

//       if (elementTop < triggerOffset) {
//           element.classList.add(animationClass);
//       }
//   });
// }

// const elementsToAnimate = document.querySelectorAll('.sub-title, .animate-image, .animate-text, .img1, .img2, .img3, .img4');

// window.addEventListener('scroll', () => {
//   animateOnScroll(elementsToAnimate, 'animate');
// });

